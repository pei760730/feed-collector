{
  "name": "feed-collector",
  "version": "0.1.0",
  "description": "feed-collector —— Telegram 短影音收集/佇列 bot(取代 n8n 版):解析→清理→FB 轉址解開→判平台→抽 video ID→去重→寫 Google Sheet「暫存區」(pending_review / unsupported 狀態流,供下游 worker 接手)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "drain": "tsx src/drain.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "dotenv": "^16.4.7",
    "googleapis": "^144.0.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
